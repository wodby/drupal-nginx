version: "2"

services:
  nginx:
    image: $IMAGE
    environment:
      NGINX_BACKEND_HOST: drupal
    depends_on:
      - drupal
    volumes:
      - codebase:/var/www/html
  drupal:
    image: drupal:7-fpm
    volumes:
      - codebase:/var/www/html

volumes:
  codebase: